function Event(name, date, blocks, people, timedate, task) {

  this.Name = name
  this.Date = date
  this.Blocks = blocks
  this.People = people
  this.TimesObjects = timedate
  this.Tasks = task
  
}

