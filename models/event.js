function Event(name, date, blocks, people, timedate,tasks) {

  this.Name = name;
  this.Date = date;
  this.Blocks = blocks;
  this.People = people;
  this.TimesObjects = timedate;
  this.Tasks = tasks
  
}